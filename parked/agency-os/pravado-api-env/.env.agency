# ==============================================================================
# PARKED ARTIFACT — awaiting activation (likely obsolete)
# ==============================================================================
# This file was originally appended to apps/api/.env in Pravado's repo so that
# Pravado's API could consume AgencyOS-related webhooks/n8n credentials. Under
# the post-D026 architecture, AgencyOS instead authenticates to Pravado's
# public API via first-party API keys — so this cross-venture env injection
# pattern may be obsolete at activation time.
#
# Original values rotated 2026-04-22 and are no longer valid. Placeholders
# inserted pending activation design decision.
#
# See parked/README.md for activation plan and Pravado D026 for architecture.
# ==============================================================================

# Agency OS — originally appended to apps/api/.env
# Consider whether this pattern is still needed post-activation.

N8N_API_KEY=<SET_AT_ACTIVATION>
N8N_BASE_URL=https://n8n.srv816212.hstgr.cloud
N8N_WEBHOOK_SECRET=<SET_AT_ACTIVATION>
N8N_VIDEO_PIPELINE_WEBHOOK=https://n8n.srv816212.hstgr.cloud/webhook/sapient-video-trigger
AGENCY_OS_API_SECRET=<SET_AT_ACTIVATION>
